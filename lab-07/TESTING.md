# Подробное руководство по тестированию Lab 7

## Установка и запуск

1. Установите зависимости (если еще не установлены):
```bash
sudo apt install cmake build-essential libgl1-mesa-dev freeglut3-dev libglu1-mesa-dev
```

2. Перейдите в папку проекта:
```bash
cd /home/iwhanter/compgraph/lab-07
```

3. Скомпилируйте проект (если еще не скомпилирован):
```bash
g++ -o lab7 main.cpp -lGL -lGLU -lglut -std=c++11
```

4. Запустите программу:
```bash
./lab7
```

Откроется окно OpenGL с отображением куба по умолчанию.

---

## 1. Тестирование загрузки и сохранения OBJ файлов

### Тест 1.1: Загрузка существующего OBJ файла

1. В окне программы нажмите клавишу `l` (или `L`)
2. В консоли введите имя файла: `example_cube.obj`
3. Нажмите Enter
4. **Ожидаемый результат**: Модель куба загрузится и отобразится в окне

### Тест 1.2: Сохранение модели в OBJ файл

1. Создайте модель (например, нажмите `1` для куба или `2` для икосаэдра)
2. Примените какие-нибудь преобразования (например, `s` для масштабирования)
3. Нажмите клавишу `w` (или `W`)
4. В консоли введите имя файла (например, `my_model.obj`) или нажмите Enter для сохранения как `mode.ol.obj`
5. **Ожидаемый результат**: 
   - В консоли появится сообщение "Сохранено: X вершин, Y граней в filename.obj"
   - Файл появится в папке проекта

### Тест 1.3: Загрузка сохраненного файла

1. Сохраните модель (как в тесте 1.2)
2. Измените модель (например, нажмите `2` для икосаэдра)
3. Нажмите `l` и загрузите сохраненный файл
4. **Ожидаемый результат**: Модель восстановится в том же состоянии, в котором была сохранена

### Тест 1.4: Проверка формата OBJ файла

1. Откройте сохраненный `.obj` файл в текстовом редакторе
2. **Ожидаемый результат**: 
   - Файл должен содержать строки с вершинами (начинаются с `v`)
   - Файл должен содержать строки с гранями (начинаются с `f`)
   - Формат: `v x y z` для вершин, `f 1 2 3 4` для граней

---

## 2. Тестирование построения фигуры вращения

### Тест 2.1: Построение фигуры вращения с примером образующей

1. В окне программы нажмите клавишу `r`
2. В консоли появится вопрос: "Использовать пример образующей? (y/n):"
3. Введите `y` и нажмите Enter
4. Введите ось вращения: `z` (или `x`, `y`)
5. Введите количество разбиений: `20` (или нажмите Enter для значения по умолчанию)
6. **Ожидаемый результат**: 
   - В окне появится фигура вращения (форма бутылки/вазы)
   - Модель будет состоять из множества граней, образующих плавную поверхность

### Тест 2.2: Построение фигуры вращения с собственной образующей

1. Нажмите клавишу `r`
2. Введите `n` (не использовать пример)
3. Введите количество точек: `5`
4. Введите координаты точек (по одной строке для каждой точки):
   ```
   0.0 0.0 0.0
   0.3 0.5 0.0
   0.5 1.0 0.0
   0.3 1.5 0.0
   0.0 2.0 0.0
   ```
5. Выберите ось вращения: `z`
6. Введите количество разбиений: `30`
7. **Ожидаемый результат**: Создастся фигура вращения по заданной образующей

### Тест 2.3: Вращение вокруг разных осей

Повторите тест 2.1, но попробуйте разные оси:
- `x` - вращение вокруг оси X
- `y` - вращение вокруг оси Y  
- `z` - вращение вокруг оси Z

**Ожидаемый результат**: Фигура будет вращаться вокруг выбранной оси

### Тест 2.4: Разное количество разбиений

Повторите тест 2.1 с разными значениями разбиений:
- `10` - грубая модель
- `20` - средняя детализация
- `50` - высокая детализация

**Ожидаемый результат**: Чем больше разбиений, тем более плавная поверхность

### Тест 2.5: Сохранение фигуры вращения

1. Создайте фигуру вращения (тест 2.1)
2. Нажмите `w` и сохраните как `revolution.obj`
3. Загрузите сохраненный файл (`l` → `revolution.obj`)
4. **Ожидаемый результат**: Фигура вращения загрузится корректно

---

## 3. Тестирование построения графика двух переменных

### Тест 3.1: Построение графика функции 1

1. Нажмите клавишу `s`
2. Выберите функцию: `1`
3. Введите диапазон X: `-3 3`
4. Введите диапазон Y: `-3 3`
5. Введите количество разбиений по X: `30` (или Enter для 20)
6. Введите количество разбиений по Y: `30` (или Enter для 20)
7. **Ожидаемый результат**: 
   - Построится поверхность функции sin(sqrt(x²+y²))/(sqrt(x²+y²)+1)
   - Будет видна волнообразная поверхность

### Тест 3.2: Построение графика функции 2

1. Нажмите `s`
2. Выберите функцию: `2`
3. Введите диапазон X: `-5 5`
4. Введите диапазон Y: `-5 5`
5. Используйте значения по умолчанию для разбиений (Enter)
6. **Ожидаемый результат**: Построится поверхность функции sin(x)*cos(y)

### Тест 3.3: Построение графика функции 3 (параболоид)

1. Нажмите `s`
2. Выберите функцию: `3`
3. Введите диапазон X: `-2 2`
4. Введите диапазон Y: `-2 2`
5. Введите разбиения: `25` для X и Y
6. **Ожидаемый результат**: Построится параболоид (x² + y²)

### Тест 3.4: Разные диапазоны и разбиения

Попробуйте разные комбинации:
- Маленький диапазон, много разбиений: X=[-1,1], Y=[-1,1], разбиения=50
- Большой диапазон, мало разбиений: X=[-10,10], Y=[-10,10], разбиения=10
- Несимметричные диапазоны: X=[-2,5], Y=[-3,3]

**Ожидаемый результат**: Поверхность строится согласно заданным параметрам

### Тест 3.5: Сохранение графика функции

1. Создайте график функции (тест 3.1)
2. Нажмите `w` и сохраните как `surface.obj`
3. Загрузите файл (`l` → `surface.obj`)
4. **Ожидаемый результат**: График функции загрузится корректно

---

## 4. Тестирование аффинных преобразований

### Тест 4.1: Преобразования загруженной модели

1. Загрузите модель (`l` → `example_cube.obj`)
2. Примените преобразования:
   - `t` - смещение вправо
   - `T` - смещение влево
   - `s` - увеличение масштаба
   - `S` - уменьшение масштаба
   - `x` - поворот вокруг X
   - `y` - поворот вокруг Y
   - `z` - поворот вокруг Z
3. **Ожидаемый результат**: Модель преобразуется в реальном времени

### Тест 4.2: Преобразования фигуры вращения

1. Создайте фигуру вращения (`r` → пример → `z` → 20)
2. Примените преобразования:
   - `c` - масштабирование от центра
   - `C` - уменьшение от центра
   - `m` - отражение по X
   - `n` - отражение по Y
   - `b` - отражение по Z
3. **Ожидаемый результат**: Фигура преобразуется корректно

### Тест 4.3: Преобразования графика функции

1. Создайте график функции (`s` → 1 → диапазоны → разбиения)
2. Примените повороты:
   - `x`, `X` - чтобы увидеть форму поверхности
   - `y`, `Y` - для разных ракурсов
   - `z`, `Z` - для вращения
3. Используйте стрелки для вращения камеры
4. **Ожидаемый результат**: Можно рассмотреть форму поверхности с разных сторон

### Тест 4.4: Сброс преобразований

1. Примените несколько преобразований к модели
2. Нажмите `R` (большая буква)
3. **Ожидаемый результат**: Все преобразования сбрасываются, модель возвращается в исходное состояние

---

## 5. Тестирование переключения проекций

### Тест 5.1: Переключение между проекциями

1. Создайте любую модель (например, график функции)
2. Нажмите `p` для переключения проекций
3. Повторите несколько раз
4. **Ожидаемый результат**: 
   - Переключение между перспективной и аксонометрической проекциями
   - В окне отображается текущий тип проекции

---

## 6. Комплексное тестирование

### Тест 6.1: Полный цикл работы

1. Создайте фигуру вращения (`r` → пример → `z` → 20)
2. Примените преобразования (`s`, `x`, `y`)
3. Сохраните модель (`w` → `test_revolution.obj`)
4. Создайте график функции (`s` → 1 → диапазоны → разбиения)
5. Примените преобразования
6. Сохраните модель (`w` → `test_surface.obj`)
7. Загрузите первую модель (`l` → `test_revolution.obj`)
8. Загрузите вторую модель (`l` → `test_surface.obj`)
9. **Ожидаемый результат**: Все операции выполняются корректно

### Тест 6.2: Работа с разными моделями

1. Создайте куб (`1`)
2. Примените преобразования и сохраните (`w` → `cube.obj`)
3. Создайте икосаэдр (`2`)
4. Примените преобразования и сохраните (`w` → `icosahedron.obj`)
5. Создайте фигуру вращения (`r`)
6. Сохраните (`w` → `revolution.obj`)
7. Создайте график функции (`s`)
8. Сохраните (`w` → `surface.obj`)
9. Загрузите все сохраненные модели по очереди (`l`)
10. **Ожидаемый результат**: Все модели загружаются и отображаются корректно

---

## Быстрые команды для тестирования

### Минимальный набор тестов:

```bash
# 1. Запуск программы
./lab7

# В программе:
# 2. Загрузка примера
l → example_cube.obj

# 3. Сохранение
w → test.obj

# 4. Фигура вращения
r → y → z → 20

# 5. График функции
s → 1 → -3 3 → -3 3 → 30 → 30

# 6. Преобразования
s (масштаб) → x (поворот X) → y (поворот Y) → p (проекция)
```

---

## Проверка результатов

### Что должно работать:

✅ Загрузка OBJ файлов  
✅ Сохранение моделей в OBJ формат  
✅ Построение фигуры вращения с примером и собственной образующей  
✅ Построение графика трех функций  
✅ Применение всех аффинных преобразований  
✅ Переключение проекций  
✅ Вращение камеры стрелками  
✅ Сохранение и загрузка созданных моделей  

### Возможные проблемы:

- Если модель не отображается: проверьте, что окно OpenGL открылось
- Если файл не загружается: проверьте, что файл существует в папке проекта
- Если преобразования не работают: убедитесь, что модель создана/загружена
- Если программа не компилируется: проверьте установку зависимостей

---

## Примеры файлов для тестирования

Создан пример файла `example_cube.obj` в папке проекта.

Вы можете создать свои OBJ файлы в текстовом редакторе:
```
# Вершины
v x1 y1 z1
v x2 y2 z2
...
# Грани (индексы вершин, начиная с 1)
f 1 2 3 4
f 5 6 7 8
...
```

Удачи в тестировании! 🚀

